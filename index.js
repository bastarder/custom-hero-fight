let hero = new Unit({
  tags: [ new Tag('hero'), ]
})

let monster = new Unit({
  tags: [ new Tag('monster'), ]
})

console.log(hero, monster)

function action(origin, target){

}


action(hero, monster)
